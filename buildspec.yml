version: 0.2

phases:
  pre_build:
    commands:
#      - npm install
      - npm i --target_arch=x64 --target_platform=linux
  build:
    commands:
      - npm run build
artifacts:
  type: zip
  files:
    - '**/*'
cache:
  paths:
    - 'node_modules/**/*'
